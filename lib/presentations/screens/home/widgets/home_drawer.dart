import 'package:flutter/material.dart';

class YHomeDrawer extends StatelessWidget {
  const YHomeDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Drawer(child: Center(child: Text('Drawer')));
  }
}
